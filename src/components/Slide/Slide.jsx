const Slide = ( {imageUrl, hoverImageUrl, linkSpan, linkUrl} ) => {

    return <div>
        imageUrl: {{imageUrl}}
        hoverImageUrl: {{hoverImageUrl}}
        linkSpan: {{linkSpan}}
        linkUrl: {{linkUrl}}
    </div>;
};

export default Slide;